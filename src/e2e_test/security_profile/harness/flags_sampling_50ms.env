# Single-knob profile — only kSamplingPeriod is changed. Used to
# isolate Stirling's perf-buffer poll cadence as the detection knob.
# Compare against flags_default.env (no override -> 200 ms compiled
# default).
PX_STIRLING_SOCKET_TRACER_SAMPLING_PERIOD_MS=50
