# Security-profile findings

Filled in after the sweep finishes. Layout matches what we'll paste
into the PR description.

## Coverage matrix

(Generated by `harness/stats.py results/summary.csv`.)

<!-- COVERAGE_TABLE -->

## Per-profile per-cell stats

| profile | N | rate (q/s) | sent_unique | seen_unique | captured_unique | coverage % | unresolved_rows |
|---|---:|---:|---:|---:|---:|---:|---:|
<!-- RAW_ROWS -->

## What the default profile cannot see

To be filled in: which (N, R) cells dropped coverage below 100 %, and
the dominant failure mode (BPF perf-buffer overrun, control-bw cap,
table-store eviction, or conn-eviction at kDeathCountdownIters).

## What flags alone achieved

To be filled in: comparison of `security_runtime` vs `default`, with
the smallest set of envvars that recovers full coverage at the largest
swept N × R.

## What flags alone could not recover

To be filled in: cells where even `security_aggressive` lost rows, and
the next code-level lever (samplingPeriod, kDeathCountdownIters, or
the per-protocol perf-buffer carve-out).

## PEM CPU/memory cost per profile

Per-cell snapshots in `results/<profile>/N=…_R=…/pem-top.txt`.
